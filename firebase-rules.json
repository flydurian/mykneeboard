{
  "rules": {
    "users": {
      "$uid": {
        ".read": "$uid === auth.uid",
        ".write": "$uid === auth.uid",
        "flights": {
          "$year": {
            "$month": {
              "$flightId": {
                ".read": "$uid === auth.uid",
                ".write": "$uid === auth.uid"
              }
            }
          }
        }
      }
    },
    ".read": false,
    ".write": false
  }
}
